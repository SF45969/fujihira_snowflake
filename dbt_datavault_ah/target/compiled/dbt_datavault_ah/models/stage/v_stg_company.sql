

-- Generated by AutomateDV (formerly known as dbtvault)

    

WITH source_data AS (

    SELECT

    COMPANY_CD,
    COMPANY_NM,
    LOAD_DATE,
    RECORD_SOURCE

    FROM S45969_DBT_DB.S45969_DBT_AH_SCHEMA.raw_company
),

derived_columns AS (

    SELECT

    COMPANY_CD,
    COMPANY_NM,
    RECORD_SOURCE AS RECORD_SOURCE,
    LOAD_DATE AS LOAD_DATE,
    LOAD_DATE AS EFFECTIVE_FROM

    FROM source_data
),

hashed_columns AS (

    SELECT

    COMPANY_CD,
    COMPANY_NM,
    LOAD_DATE,
    RECORD_SOURCE,
    EFFECTIVE_FROM,

    CAST(MD5_BINARY(NULLIF(UPPER(TRIM(CAST(COMPANY_CD AS VARCHAR))), '')) AS BINARY(16)) AS COMPANY_PK,

    CAST(MD5_BINARY(IFNULL(NULLIF(UPPER(TRIM(CAST(COMPANY_NM AS VARCHAR))), ''), '^^')) AS BINARY(16)) AS COMPANY_HASHDIFF

    FROM derived_columns
),

columns_to_select AS (

    SELECT

    COMPANY_CD,
    COMPANY_NM,
    RECORD_SOURCE,
    LOAD_DATE,
    EFFECTIVE_FROM,
    COMPANY_PK,
    COMPANY_HASHDIFF

    FROM hashed_columns
)

SELECT * FROM columns_to_select